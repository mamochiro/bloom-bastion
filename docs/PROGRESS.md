# Bloom Bastion — Progress & Decision Log

> Milestone tracker + append-only decision log. CLAUDE.md keeps only the locked
> rules + a one-line status snapshot and points here. See SPEC §13 for the full
> roadmap, `docs/decisions/` for ADRs.

---

## 🎯 Milestones

**Current: M1 — Core Loop** (essentially complete; remaining = real wave system).
The game is fully playable + replayable: place towers → fight waves → win/lose → replay.

### M0 — Foundation (done)
- [x] Scaffold with Bun + Vite 6 + TS strict + Pixi v8 + React 19 + Biome
- [x] Integrate design tokens (from `design-assets/export/`, see ADR-0002)
- [x] Pixi app boots, renders dark battlefield + design-token grid
- [x] Vitest wired; Playwright dep installed
- [x] Smoke test: 1 tower + 1 enemy walking the flow-field path, rendered (delivered in M1)
- [x] Playwright e2e config + first happy-path test (`playwright.config.ts` mobile-first Pixel-5 393×851 + reuse dev server; `tests/e2e/happy-path.spec.ts` GREEN 1 passed). **Real-browser runtime proof:** `#game` mounts a real `<canvas>` (boundingBox >0 = Pixi rendered, WebGPU→WebGL fallback clean); boot→Normal→Play→place Blossom (150→100g)→live 4s wave with `page.on(pageerror)`+console-error asserted EMPTY (the white-screen check jsdom can't give). Resilient selectors only (no `data-testid`/ui edits)
- [ ] Deploy preview to Vercel

### M1 — Core Loop (in progress)
- [x] bitECS world + all §4.2 components; RAF loop (dt-clamp 50ms, Page Visibility pause); locked §4.2 10-system pipeline via `startLoop(GAME_SYSTEMS)` seam
- [x] Flow-field pathfinding (Dijkstra cost-spread, 8-neighbour, NOT A*); recompute on map load + tower placement
- [x] RenderSystem (slot 9) — per-eid Pixi **Sprite from the real SVG atlas** (`getSpriteTexture(spriteId)`), zero per-frame alloc, anchors (towers 0.5/1.0 grounded · enemies/projectiles 0.5/0.5), tint FX-only (hit-flash)
- [x] Object pools (§4.4): particle 500 / projectile 100 / enemy 200 / floating-text 50
- [x] SpawnSystem (slot 2) + PathFollowSystem (slot 3); enemy spawns → walks lane → despawns at goal
- [x] Config/factories: Grub enemy + Blossom tower (exact SPEC §6); design-token tints
- [x] Combat: TowerAISystem (4) + ProjectileSystem (5) + DamageSystem (6) + DeathSystem (7) — targets nearest-in-range, fires homing projectiles, damage + 40%/2s slow, kills → gold
- [x] `gameTime()` pause-aware game clock (engine/loop.ts) for Status slow/stun/dot timestamps
- [x] Pool integration: enemy + projectile factories acquire/release from pools (Hit tag component)
- [x] ECS economy: `Resources{gold,lives}` game singleton (ECS-pure), SPEC §6.5 Normal = 150 gold / 20 lives
- [x] Lives accounting: PathFollow goal-leak calls `loseLives(world, 1)` (LIFE_COST_PER_LEAK=1, NOT-LOCKED — SPEC §6 only defines lose-at-0)
- [x] UISyncSystem (slot 10) → ≤10Hz throttled `GameSnapshot` into Zustand store (`src/store/game-snapshot.ts`, snapshot-only mirror)
- [x] HUD overlay (`src/ui/hud/`) — gold/lives/wave strip; DOM-over-canvas, design tokens, safe-area, a11y
- [x] InputSystem (slot 1) — engine pointer/tap primitive (`src/engine/input/`, world px == Position space, lazy-attach) + gameplay placement (tap grass → validate `COST_GRASS` + gold ≥ cost → spend, `placeTower`, `COST_BLOCKED`, rebuild flow field)
- [x] TowerPicker (`src/ui/hud/TowerPicker.tsx`) + build-intent store (`src/store/build.ts`)
- [x] Visual pass (design-dev): runtime SVG atlas (`public/sprites.svg` + `public/sprites/*.svg`, id-banded registry in `config/sprites.ts`); real TowerCard glyph; `--fs-*` type scale (zero raw px font-size in `src/ui`)
- [x] Complete game loop: win/lose/pause/restart — authoritative phase (`src/game/ecs/game-state.ts`); win/lose folded into DeathSystem (§6.6, no new §4.2 slot); sim freezes via `isSimPaused()` guards (slots 1–7; Render/UISync keep running); `restartGame` (`src/game/restart.ts`); victory/defeat overlays + Play Again (`src/store/commands.ts`)
- [ ] Death juice (particle/floating-text pools wired but DeathSystem doesn't spawn them yet)
- [ ] Visual follow-ups (design-dev): tiles **board layer** (grass/path 300/301 assets ready, not drawn); **animated glow** (static SVG only — Pixi tween/Rive later); convert other 5 towers ×3 + 7 enemies (mechanical); formalize an **enemy-palette** token set
- [x] **Wave system (ADR-0003 / SPEC §6.3): data-driven waves 1–3.** `src/game/config/waves.ts` (`Wave = SpawnGroup[]`); SpawnSystem generalized to multi-wave/multi-group (`getCurrentWave`/`isWaveComplete`/`isLastWave`/`TOTAL_WAVES`/`reset`); 3s inter-wave auto-advance (phase stays `playing`); §6.5 wave-clear economy (25+5·wave bonus + 5% interest cap 50); **Snail** enemy (180hp/0.5/18g, sprite id 101) + **armor** mechanic in DamageSystem (`dmg·(1−armor)`); last-wave win guard in DeathSystem (no false win after wave 1/2)
- [x] **Tint is FX-only (locked) — enforced.** Factories set `Renderable.tint=0` at spawn → atlas sprites show native SVG colors; non-zero tint reserved for hit-flash/status FX; `cfg.tint` removed; regression guard `tests/unit/game/factory-tint.test.ts`. (Fixed a latent visual bug from the visual-pass slice that no `bun run check` caught.)
- [x] **2nd tower: Stormcloud (chain lightning), §6.1.** 20dmg/2.2/1.0s/100g; `Projectile.special` bitflags (`Slow`/`Chain`), TowerAI builds special from config; chain resolves in DamageSystem (primary full + 2 nearest @50% within radius, zero-alloc top-2 scan) — NO new §4.2 slot; `tower-stormcloud-l1` sprite (id 2); TowerPicker generalized to a card-per-`PLACEABLE_TOWERS` (Blossom 50g + Stormcloud 100g). First real strategic tower choice (slow-single-target vs chain-AoE).
- [x] **Start screen + difficulty select (the game's front door).** `GamePhase`/`gameStatus` gains `'menu'` (boots frozen, board visible); start-screen overlay (title + Casual/Normal/Hardcore radiogroup + Play); §6.5 difficulty applied — `config/difficulty.ts` sets starting gold/lives + enemy HP/speed multipliers (cached mods, zero-alloc), `startGame(difficulty)`/restart consumed in InputSystem (no new §4.2 slot); `main.tsx` de-hardcoded (auto-placed Blossom + auto-start removed).
- [x] **Active skills (§6.4).** 3 skills in `config/skills.ts` — **Meteor** 🌠 (tap-to-aim 200 AoE/2-tile, 60s, default), **Freeze All** ❄️ (stun 3s, 90s, unlock wave 5), **Gold Rush** 💰 (2× gold 10s, 120s, unlock wave 10); ECS skill-state module (`readyAt` timestamp cooldowns, zero-alloc), `activateSkill` consumed in InputSystem (aim-beats-placement); Freeze **implements the previously-unused `Status.stunnedUntil`** (PathFollow holds stunned enemies), Gold Rush 2× in DeathSystem; skill bar (radial cooldown + lock hints) reads `snapshot.skills[]`. NO new §4.2 slot.
- [x] **Candy King mini-boss (§6.2) + waves extended to 10.** CandyKing enemy (1200hp/0.7/100g, `enemy-candyking` sprite id 102); **boss-phase system** — `Enemy.flags` bits, fire-once in DamageSystem: at 50% HP summon 4 Grubs + 1.2× speed, at 25% berserk 1.4× + **slow-immune** (PathFollow ignores `Status.slowedUntil`); boss HP bar (`snapshot.boss`, progressbar a11y). WAVES 4–10 (Candy King at 5 & 10, Grub/Snail ramps elsewhere). **Reaching waves 5/10 now unlocks Freeze + Gold Rush in real play.** NO new §4.2 slot. *(Survived a mid-slice API outage — both agents recovered via resume nudges.)*
- [x] **Flutter + Splitter enemies (§6.2/§6.3) — waves 4/9 now §6.3-faithful.** Flutter (50hp/2.0/12g, `enemy-flutter` 103, **flying** — `ENEMY_FLAGS.Flying`, immune to Meteor AoE/ground-splash but hit by single-target + chain); Splitter (160hp/1.0/22g, `enemy-splitter` 104) **splits on death** (DeathSystem spawns 2 non-resplitting MiniSplitters on the flow field); MiniSplitter (40/1.2/8g, reuses 104, stats tuned-flagged). NO new §4.2 slot.
- [x] **Shade + Plushy enemies (§6.2).** Shade (90hp/1.6/15g, `enemy-shade` 105, **20% dodge** — `applyDamage` rolls an injectable RNG; covers projectile/chain/Meteor since it's the single damage chokepoint; non-dodgers short-circuit; `resetDamageRng` for test isolation); Plushy (150hp/0.9/20g, `enemy-plushy` 106, **regen 5 HP/s** in PathFollow, clamped, no revive — a burst-or-bust tank). Sprinkled into the §6.3 waves 6–8 mix. NO new §4.2 slot. **All trash + mini-boss enemies now exist (7 of 8 §6.2 types — only the Neon Dragon final boss remains).**
- [x] **Neon Dragon final boss (§6.2) + waves 11–20 — CAMPAIGN COMPLETE.** Neon Dragon (4000hp/0.8/300g, `enemy-neon-dragon` 107, isBoss); **P2** at 50% HP gains Flying + 1.5× speed (Candy King threshold-phase system extended with `setFlying`); **P1 recurring summon** (NEW mechanic) — a Flutter every 5s via an eid-keyed gameTime timer in PathFollow, stops at P2, `resetBossSummon` for isolation. Waves extended to 20 (11–13 fast density, 14 Plushy-heavy, 15 Candy King + Splitters, 16–19 hardcore mix, 20 Neon Dragon); Candy King now at 5/10/15. **Beating wave 20 → "BLOOM PREVAILS" (real campaign victory).** NO new §4.2 slot. §6.2 P2 "Plushies on tower death" SKIPPED (no tower-death mechanic) — flagged.
- [ ] **Endless mode** (§6.3 post-wave-20: HP×1.15ⁿ, count×1.05ⁿ, boss every 5, leaderboard)
- [ ] Towers 3–6 (Sugar Cannon, Luna, Hive, Bubbler) + L2/L3 upgrades — same pipeline
- [ ] **SPEC clarifications flagged:** Stormcloud chain radius (`CHAIN_RADIUS_TILES=2.5`, §6.1 no arc range); Meteor damage raw-vs-armor (§6.4 says "200 DMG"; applied armor-adjusted for consistency)
- [ ] **Enemy-palette tokens (design-dev rec → small ADR):** enemy colors are a mix of borrowed tower tokens (`--storm-*`) + un-tokenized grub literals; formalize an `--enemy-*` token set (SPEC §2.3 change)
- [ ] **Tower death / lose-condition for towers** — needed for the Neon Dragon P2 "Plushies on tower death" bit (currently unbuildable)
- [x] **VFX juice (pass 1) — AnimationSystem (slot 8) now REAL → pipeline 10/10.** Pooled (§4.4) particle bursts + floating reward text + hit-flash + Meteor impact, all zero-per-frame-alloc (pre-alloc TypedArrays + swap-remove pool release). `Renderable.flashUntil` field (transient FX tint, base stays 0); gameplay hooks via an injectable `VfxSink` (flash on landing hit not dodge, death burst+gold, meteor boom, dragon P2 enrage burst); `setVfx` wired in `main.tsx` at boot. Game clock extracted to `engine/clock.ts` (breaks the loop↔vfx cycle; `loop` re-exports `gameTime`).
- [x] **VFX juice (pass 2).** Animated idle sprite motion (enemy bob, tower/projectile scale-pulse — render-only offset, zero-alloc, de-synced per eid); **§4.5 adaptive quality** (rolling-FPS bucket → `setVfxQuality` per the spec table with ±3fps hysteresis, `getQuality`/`getAverageFps`); `Renderable.baseTint` (persistent FX tint — flash returns to baseTint not 0) → **Neon Dragon P2 enrage red**; **skill screen flash** (ui, derived from the skills snapshot ready→fired edge, pointer-events:none, reduced-motion softened).
- [ ] Settings **Auto/Low/High** quality UI (§4.5 — wire to `setVfxQuality`/`getQuality`); §4.5 bloom/chromatic toggles (no shader yet — particle scaling only so far)
- [ ] DoT hooks (`Status.dotUntil` still unused); retire the engine `Sprite` back-compat alias; ui wave-counter "X/N"; cosmetic: `pipeline.ts:37` AnimationSystem comment still says "(engine stub)" (stale — it's real); optional Candy King draw-scale; **balance playtest tuning**

**Gate (last):** `bun run check` green (Biome 98 · tsc strict · Vitest 228/228 · build) + 5× `--sequence.shuffle` clean. Bundle ≈240KB gz (app 75 + pixi 160 + react 4; budget <400KB). SVG art ships as separate files (`dist/sprites/`), NOT in the JS bundle. **Pipeline: 10/10 systems REAL — no stubs.** **Content COMPLETE + juiced: start screen + 3 difficulties, 2 towers, all 8 §6.2 enemies (incl 2 bosses), 20 §6.3-faithful waves, 3 active skills, win/lose/restart, combat + ambient VFX, §4.5 adaptive quality — a full playable, polished campaign.**

---

## 📝 Decision Log

(Append-only. Newest first.)

- **2026-06-10** — 🐌 Per-projectile slow magnitude + duration landed (gameplay+qa, SPEC §6.1 fidelity) — closes the tracked debt where the slow system had ONE global magnitude (40%/2s) so Sugar L3 (should be 20%) and Bubbler (30%) wrongly reused 40%. NO new §4.2 slot. **engine:** one authorized line — `slowFactor: Types.f32` added to the `Status` component (world.ts, qa-verified as the only engine change). **gameplay:** shared `src/game/ecs/slow.ts` `applySlow(eid, reduction, durationS)` → sets `Status.slowFactor` (speed multiplier) + `slowedUntil`; ALL slow sources route through it (primary Slow, Petal Storm AoeSlow, Sugar splash SplashSlow, Bubbler Tsunami Line). Magnitudes are DATA in `TOWERS.<id>.slow` (speedReduction+durationS): Blossom 0.4/2s, Sugar 0.2/1s, Bubbler 0.3/1.5s. PathFollow scales speed by `slowFactor` while `slowedUntil>now` (replaces the fixed 0.4 constant), gated + reset on spawn. **Stacking** (NOT-LOCKED, §6-silent): strongest-magnitude-wins (`min`) + refresh-to-longer (`max` duration) — a weaker-but-longer slow extends duration without weakening an active stronger slow. Slow-vs-Push disambiguation: Bubbler projectile carries Push → 30%; plain Blossom Slow → 40% (tested both). **Now §6.1-exact:** Blossom 40%/2s · Sugar L3 20%/1s · Bubbler 30%/1.5s. Blossom feel IDENTICAL (regression-clean); Freeze stun (stunnedUntil) unaffected. **Gate:** `bun run check` exit 0 (Biome 118 · tsc · Vitest 321/321 [46 files] · build), ~245KB gz; 5× shuffle clean. SPEC §6.1 deviation flags REMOVED (Sugar/Bubbler now accurate); Bubbler 1.5s duration NOT-LOCKED (§6.1 silent).
- **2026-06-10** — ⚙️ Settings / Quality UI landed (engine+ui+qa, §4.5 + SPEC §6 Settings) — first user control over the adaptive-quality system. NO §4.2 change (engine RAF-loop seam only). **engine:** manual quality-mode override in `loop.ts` — `QualityMode = "auto"|"low"|"high"`, `setQualityMode`/`getQualityMode` (+ existing `getQuality`/`getAverageFps`). `frameStep` runs the FPS monitor ONLY when mode==="auto"; "high" pins the §4.5 vfx scale to 1.0, "low" to 0.2 (table min), bypassing the monitor; "auto" resumes FPS-driven adaptation; reset to "auto" on `startLoop`. **ui:** new `src/store/settings.ts` (thin Zustand store — qualityMode + `setQualityMode` action that updates the store AND calls the engine seam; quality is RENDER config not ECS state, so a UI store owning it is correct + touches no world/ECS) + `SettingsPanel.tsx` (modal, role=dialog, Auto/High/Low radiogroup aria-label "Render quality", live FPS/quality readout polled ~3Hz only while open, tokens-only + --fs-*, ≥56px touch) + a top-right gear on StartScreen. **Deferred (noted in code):** SPEC §6 SFX/Music/Haptics settings are out of scope until audio (AudioManager/Howler) is wired — only Quality built. **Gate:** `bun run check` exit 0 (Biome 116 · tsc · Vitest 311/311 [45 files] · build), ~245KB gz; 5× shuffle clean (qualityMode resets on startLoop; the ui vi.mock of the engine seam is file-scoped, no leak into the real adaptive-quality test). E2E now **4 specs** (+ open Settings gear → select Low → close, zero console errors).
- **2026-06-10** — ♾️ ENDLESS MODE landed (gameplay+ui+qa, SPEC §6 menu "Endless" / §6.3) — first non-tower feature; a second game mode atop the 20-wave campaign. NO new §4.2 slot (folds into SpawnSystem + DeathSystem win-skip + UISync). **Mode:** `GameMode = "campaign" | "endless"` in game-state (get/set/resetGameMode); `startGame(difficulty, mode="campaign")`; restart PRESERVES mode (Play Again replays the same mode — but every fresh menu run calls startGame→setGameMode, so no leak). **Wave-gen** (`config/waves.ts` `genEndlessWave(n)`, `ecs/endless.ts` HP-mult state): past the 20 authored waves, waves are procedurally generated — **HP ×1.15ⁿ (LOCKED)**, count ×1.05ⁿ ceil monotonic (LOCKED), boss every 5 (Neon Dragon every 10, else Candy King), DETERMINISTIC n-cycle composition (Grub/Snail/Flutter/Shade core; Splitter on even n, Plushy on n%3) — **flagged NOT-§6.3-"random" but reproducible/no-RNG**. n=1 at wave 21. **Win-skip (critical):** the `won` branch is hard-gated `!isEndless()` (death.ts) AND `SpawnSystem.isLastWave()` returns false in endless — belt-and-braces, endless ends ONLY on lives≤0; campaign wave-20 win path UNCHANGED (highest-risk regression, test-guarded). **Score** = highest wave cleared (`recordWaveCleared`), exposed via snapshot. **Contract:** `GameSnapshot` gains `mode: GameMode` + `score: number` + `useGameMode()`/`useScore()` (DEFAULT {campaign, 0}); `requestStart(mode)` command (`getStartMode`/`consumeStart`). **ui:** StartScreen Endless button (`requestStart("endless")`, shares the difficulty radiogroup) — also FIXED a latent bug where the click event was passed as the mode arg (`onPlay={()=>requestStart()}`); Hud end-screen branches on mode — campaign win → "BLOOM PREVAILS" (gated `status===won && mode===campaign`), endless loss → `EndlessOverlayView` "RUN ENDED / Wave Reached: {score}", and NOTHING claims a win in endless. **Gate:** `bun run check` exit 0 (Biome 113 · tsc · Vitest 299/299 [44 files] · build), ~244KB gz; 5× shuffle clean (resetGameMode + resetEndlessHpMult in _reset; genEndlessWave deterministic). E2E now **3 specs** (+ click the Endless button → run starts → tick an endless wave → zero console errors). Campaign + all 6 towers/8 enemies/20 authored waves UNCHANGED (endless is additive). **Scoring NOT-LOCKED:** leaderboard backend deferred. Also: `graphify-out/` added to `.gitignore` (local knowledge-graph cache, was tripping Biome).
- **2026-06-10** — 🌊 Tower #6 Bubbler (crowd-control) + L1/L2/L3 landed (design-dev+gameplay+qa, SPEC §6.1) — **THE FINAL TOWER: the full §6.1 6-tower roster × 3 levels = 18 configs is now COMPLETE** (Blossom · Stormcloud · Sugar Cannon · Luna Crystal · Hive · Bubbler). NO new §4.2 slot (push/slow/line in Damage(6), targeting/placement/UI generic). **New mechanic — push-back:** on hit, shove the target BACKWARD along its REVERSE flow-field (newPos = pos − flowDir×pushPx), 0.5t base / 1.0t with PushBig. **All-or-nothing clamp** (qa-verified safe): the full push applies only if the landing cell is on-map AND not blocked (`cost<COST_BLOCKED`), else skipped — no off-map teleport, no negative-progress-past-spawn, map-edge push is a no-op. **Sprite:** design-dev `tower-bubbler-l1` (id 6 — freed when minions moved to 400-band; aqua BUBBLER mint-cyan family, completing warm/cool/aqua roster). **Config:** base 12/2.0/0.8/80g + slow + push; L2 Tidal Wave +70g (+8→20 DMG, push 1.0t); L3 Tsunami +140g (line attack). `TowerType.Bubbler=5`. **SPECIAL bits** Push(1<<11)/PushBig(1<<12)/Line(1<<13) → **14/16 of the ui16 field used**; isolated (a plain Slow shot doesn't push — guard test). **L3 Tsunami:** hit-time lane-axis line — uncapped, all ground enemies within ±4t along / 0.5t perpendicular of the primary's lane take damage+slow+push (fliers skipped). **Mechanics:** push = scalar Position write; line = inline uncapped scan; both zero-alloc. **Flagged (SPEC §6.1):** L2 +DMG chosen as +8 (§6.1 silent); fliers ARE pushed (water shove); **slow reuses the shared global 40%/2s, NOT §6.1's 30% — per-projectile slow magnitude+duration is now wanted by Sugar L3 (20%) + Bubbler (30%) + (Blossom 40% is correct), tracked as a follow-up fidelity slice.** **Gate:** `bun run check` exit 0 (Biome 111 · tsc · Vitest 285/285 [43 files] · build), ~243.4KB gz; 5× shuffle clean (push/line deterministic, no new RNG). All 6 towers placeable (PLACEABLE_TOWERS.length===6); the other 5 towers' specials regression-clean. E2E (2 specs) green with the 6-card picker; all 6 cards asserted present in-browser. ui untouched.
- **2026-06-10** — 🐝 Tower #5 Hive (summoner) + L1/L2/L3 landed (engine+design-dev+gameplay+qa, SPEC §6.1) — the meatiest tower + the **first friendly mobile unit**. The Hive doesn't fire: it maintains a swarm of autonomous bee minions that free-fly near it and bite ground enemies. NO new §4.2 slot — summon + bee AI fold into TowerAI(4) as a sub-pass; bee bites route through shared apply-damage(6). **engine:** new pooled `Minion` component (`src/engine/ecs/world.ts`: targetEid/expiresAt/attackCdUntil/damage, reuses Position+Renderable, NO hp — lifetime governs death) + `minionPool` (§4.4, 64, strips/zeroes on release) + `minionQuery` — a pure data primitive, zero behavior (267/267 with 3 pool tests). **design-dev:** `tower-hive-l1`(id 5, honeycomb, grounded) + `minion-bee`(small 32×32 centered) + `minion-queen`(44×44 crowned) sprites. **orchestrator** resolved a cross-system id-band conflict: RenderSystem anchors id≤99 grounded / >99 centered + enemies 100–199 bob — so minions were renumbered to a new **400-band** (`minion-bee=400`, `minion-queen=401`) to render centered + scale-pulse (not tower-grounded or enemy-bob). **gameplay:** `config/hive.ts` consts + `entities/create-minion.ts` factory + TowerAI sub-pass: Hive tops its swarm up to size (3 L1 / 5 L2) every HIVE_SUMMON_COOLDOWN_S(1.0s, NOT-LOCKED); each bee seeks the nearest ground enemy within BEE_SEEK_RADIUS(2.0t), free-moves toward it (BEE_SPEED 2.5t/s, not flow-field), bites for `damage` on BEE_ATTACK_INTERVAL(0.5s) via apply-damage (dodge/armor apply); expires on lifetime → pool release. L3 also maintains 1 Queen (20 DMG, 10s, sprite 401). `TowerType.Hive=4`, cost 125g, generic upgrade/sell (125/+90/+180; sell 75/L3 158). **Flagged (documented SPEC §6.1):** minion HP omitted → "10 HP"/"+HP per bee"/"queen 50 HP" vestigial no-ops (L2 realized as swarm 3→5); bees ground-only (skip fliers); "patrol nearby" = proximity approximation (HIVE_COUNT_RADIUS 3.0t, no home-leash field). **Gate:** `bun run check` exit 0 (Biome 110 · tsc · Vitest 276/276 [42 files] · build), ~243KB gz; **5× shuffle clean — minions drained in `_reset` AND `restartGame.clearEntities`, pool active() returns to baseline (no bee leak, the tower-leak class closed for minions too)**. E2E now 2 specs: existing place+upgrade flow + a new one placing a Hive and running a 5s live wave while bees spawn/fly/expire — ZERO pageerror/console-error (real-browser proof of the new minion render path). ui untouched. **5 of 6 towers built — only Bubbler remains.**
- **2026-06-10** — 🌙 Tower #4 Luna Crystal (sniper) + L1/L2/L3 landed (design-dev+gameplay+qa, SPEC §6.1) — longest-range tower; the meatiest tower mechanic so far. NO new §4.2 slot (AntiArmor/Crit in Damage(6), Pierce fan-out in TowerAI(4), placement/UI generic via `PLACEABLE_TOWERS`). **Prereq — `SPECIAL` ui8→ui16 widen:** the `Projectile.special` field lives in `src/engine/ecs/world.ts` (not game-land); gameplay made the authorized one-line `Types.ui8→Types.ui16` change (qa-verified as EXACTLY that + doc-comment, behavior-preserving — guard test round-trips bit `1<<10`). Bits now 11/16 used (AntiArmor 1<<8, Pierce 1<<9, Crit 1<<10). **Sprite:** design-dev `tower-luna-l1` (id 4, faceted moon-crystal sniper, cool LUNA lavender token family to contrast the warm Sugar Cannon, matched 120×150/anchor). **Config:** base 60/4.0/1.5/150g + AntiArmor (all levels); L2 Pierce +100g; L3 Moonburst +200g (special-only — no per-level base change, §6.1 gives none). `TowerType.Luna=3`. **Mechanics:** AntiArmor = ×1.3 PRE-armor vs armored targets (Snail 60→78→×0.5=39, net stronger into armor), no bonus vs unarmored, via new `isArmored()` in apply-damage; Pierce = resolved at FIRE-TIME in TowerAI (the projectile loses its origin by hit-time) — zero-alloc capped line scan along the tower→target ray (BEAM_WIDTH 0.4 NOT-LOCKED), fires up to 3 beams, Pierce bit stripped from the projectiles; Moonburst crit = 25% ×2 per-target via the shared injectable `damageRoll()` RNG (order AntiArmor→crit→armor/dodge); beam reuses the projectile path (no hitscan/new VFX); the sniper HITS fliers (no flying-skip in targeting). **Economy:** generic (150/+100/+200; sell 90 un-upgraded / 180 at L3). **Gate:** `bun run check` exit 0 (Biome 107 · tsc · Vitest 264/264 [41 files] · build), ~242.2KB gz; 5× shuffle clean (crit+dodge+Overcharge share one `resetDamageRng`). Regression-verified: Overcharge stun + Petal Storm + Sugar splash all intact under the wider field. E2E: 4-card picker doesn't break the Blossom flow; Sugar Cannon + Luna cards asserted present in-browser. **Discrepancy flagged (non-blocking):** designer source lists Luna at 180g/"pierce beam" but SPEC §6.1 says 150g/single-target+AntiArmor — config follows SPEC (the contract); reconcile separately if desired. ui untouched. Towers 5–6 (Hive/Bubbler) remain.
- **2026-06-10** — 🍭 Tower #3 Sugar Cannon (AoE splash) + L1/L2/L3 landed (design-dev+gameplay+qa, SPEC §6.1) — first new tower since the upgrade system, which made its L2/L3 nearly free. NO new §4.2 slot (splash in Damage(6), targeting generic in TowerAI(4), placement/UI generic via `PLACEABLE_TOWERS`). **Sprite:** design-dev authored `tower-sugarcannon-l1` (id 3, candy-mortar + lollipop swirl, SUGAR token family, matched 120×150 viewBox/anchor of the other towers) + atlas/registry. **Config:** base 30/2.0/1.8/75g + 1.5-tile splash; L2 Bigger Boom +60g→50 DMG/2.0 splash; L3 Sticky Sugar +120g→splash slows (1s). `TowerType.SugarCannon=2`, `towerLevelStats` returns `splashRadius` per level. **SPECIAL bits (combat.ts) now 8/8 FULL** — Splash(1<<5)/SplashBig(1<<6, L2 radius)/SplashSlow(1<<7, L3); radius derived from bits (ChainPlus pattern). **Mechanic:** `aoeSplash` (damage.ts) is an UNCAPPED inline `enemyQuery` scan — full damage to EVERY ground enemy in radius (its own zero-alloc loop, NOT the capped nearest-3 scratch used by Chain/Petal Storm), distinct bit so Stormcloud Overcharge + Blossom Petal Storm are unaffected (one special-AoE per hit). **Economy:** generic upgrade/sell already covered it (75/+60/+120; sell 45 un-upgraded / 102 at L3). **Flagged deviations (documented in SPEC §6.1):** (a) splash skips fliers (Meteor precedent; a direct projectile can still strike a flier), (b) no damage falloff, (c) L3 slow reuses the shared global 40%/1s not §6.1's literal 20% (per-enemy slow-magnitude field deferred), (d) **`SPECIAL` ui8 is now full — towers 4–6 (Luna/Hive/Bubbler) need a wider field (ui8→ui16) or different encoding before adding more specials.** **Gate:** `bun run check` exit 0 (Biome 106 · tsc · Vitest 256/256 [40 files] · build), ~241.6KB gz; 5× shuffle clean (splash deterministic, no `_reset` change). E2E: 3-card TowerPicker doesn't break the Blossom flow; added an in-browser assertion the Sugar Cannon card is present. ui/engine untouched. Towers 4–6 remain unbuilt.
- **2026-06-10** — 🗼 Tower upgrade system L1→L2→L3 + sell landed (gameplay+ui+qa, SPEC §6.1/§6.7) — first strategic depth on placed towers; the 2 BUILT towers only (Blossom, Stormcloud; towers 3–6 unbuilt → their L2/L3 land with them). NO new §4.2 slot — folded into Input(1)/TowerAI(4)/Damage(6)/UISync(10). **Config:** `towers.ts` per-level model (base = L1, `upgrades:[L2,L3]` {label,cost,damage,range,cooldown}); helpers `towerLevelStats`/`upgradeInfo`/`totalInvested`/`sellValue`, `MAX_TOWER_LEVEL=3`; SPECIAL bits ChainPlus/Stun/AoeSlow (combat.ts). Blossom L2 Bigger Bloom +40g→25/2.8/1.0, L3 Petal Storm +80g→on-hit AoE (damage+40%/2s slow splash to 3 nearest within 1 tile); Stormcloud L2 Static Field +75g→35dmg/chain 3→4, L3 Overcharge +150g→20% stun/hit. **Mechanics:** TowerAI reads `towerLevelStats(typeId, Tower.level)`; DamageSystem generalized nearest-N scan (zero-alloc module scratch `_nearEid`/`_nearDist`, ≤3, Chain XOR AoeSlow per hit); Overcharge stun via the EXISTING injectable `damageRoll()` RNG → `Status.stunnedUntil` (reuses the Freeze stun path PathFollow honors). **Economy §6.7:** `upgradeTower` gold-guarded; `sellTower` refunds `floor(totalInvested × (level===1?0.6:0.4))` (60% never-upgraded / 40% if upgraded, DERIVED from typeId+level — no new ECS field) + frees the cell (COST_GRASS) + rebuilds the flow field + releaseTower. **Input (slot 1):** tap a tower cell → SELECT (zero-alloc `towerAt`, priority over placement); tap buildable grass + build-intent → place; tap elsewhere → clear. **Contract:** `GameSnapshot.selectedTower` (eid/name/level/upgrade{label,cost}|null at L3/sellValue) + `useSelectedTower()` + commands `requestTowerUpgrade/Sell/ClearSelection` — gameplay defined it first so ui built against the real shape (no interim-type churn). **ui:** `UpgradePanel.tsx` non-blocking bottom drawer (wrapper `pointer-events:none` so taps fall through to canvas for tower-to-tower reselect; no tap-eating backdrop), name+level, Upgrade `label·cost` (greyed/disabled when gold<cost), MAX state at L3, Sell `+Ng`, X→clear; `role=dialog`, tokens-only + `--fs-*`, ≥56px touch, safe-area; mounted only during `playing`. **Gate:** `bun run check` exit 0 (Biome 105 · tsc · Vitest 247/247 [39 files] · build), ~241.4KB gz <400; 5× `--sequence.shuffle` clean (resetDamageRng covers BOTH dodge + Overcharge stun — same `apply-damage` rng; resetSelection added to `_reset.ts`). **E2E extended** (`tests/e2e/happy-path.spec.ts`): place Blossom (150→100g) → re-tap cell → UpgradePanel `role=dialog` → Upgrade → gold 100→60 + "Lv 2" → live 4s wave, ZERO pageerror/console-error throughout = real-browser proof of the upgrade loop. **NOT-LOCKED flagged in §6.1:** Blossom faster-fire 1.0s, Petal Storm 3 targets/1-tile, Overcharge stun 1.0s (20% chance LOCKED) — kept tunable.
- **2026-06-10** — 🎭 First Playwright E2E happy-path landed (qa) — **M0 COMPLETE; first real-browser runtime proof.** Closes the last open M0 item. `playwright.config.ts` (testDir `./tests/e2e`, baseURL :5173, ONE chromium-mobile project `devices["Pixel 5"]` 393×851 portrait per the mobile-first SPEC, `webServer: bun run dev` + `reuseExistingServer` + 30s timeout; chromium installed, Playwright 1.60) + `tests/e2e/happy-path.spec.ts` → `bun run test:e2e` 1 passed (6.1s). Proves in a real browser what 228 jsdom unit tests cannot: `#game` mounts a real `<canvas>` (boundingBox w/h >0 = **Pixi actually rendered**, WebGPU→WebGL fallback clean in headless chromium) → StartScreen → Normal → Play (start dialog gone, Skills bar visible, HUD gold pill "150 gold" = §6.5 Normal economy) → select Blossom + tap grass (3,2) → gold 150→100 (placement + InputSystem + unfrozen sim) → live ~4s wave (spawn/path-follow/tower-ai/death ticking). **Boot-proof (the white-screen check):** `page.on(pageerror)` + console-error collected across the ENTIRE flow asserted EMPTY — ZERO uncaught exceptions through boot + a live wave. No `data-testid`/`src/ui` edits — resilient selectors only (`getByRole`/aria-label/text + `canvas#game`); gold-pill scoped to `div[aria-label$="gold"]` to disambiguate from TowerPicker buttons' "— N gold" labels. Gate intact after the config addition: `bun run check` exit 0 (Biome 100 · tsc · Vitest 228/228 · build), ~240KB gz. **Note:** `enemiesAlive` isn't surfaced in the DOM HUD, so wave-progression is proven by the zero-error survival of a 4s live sim rather than a DOM readout (non-flaky without a ui hook).
- **2026-06-09** — VFX juice pass 2 landed (engine+gameplay+ui+qa). engine: animated idle sprite motion (enemy vertical bob, tower/projectile scale-pulse — a render-only offset on `sprite.y`/`.scale`, Position never mutated, zero-alloc scalar `sin`, de-synced per eid) + **§4.5 adaptive quality** (rolling-FPS zero-alloc bucket → `setVfxQuality` per the table `>=55:1.0 / 45-54:0.5 / 35-44:0.25 / <35:0.2`, ±3fps hysteresis so it won't thrash; `getQuality`/`getAverageFps`; reset on `startLoop`; particle-count scaling — bloom toggles N/A, no shader yet) + `Renderable.baseTint` (persistent FX tint that survives a hit-flash: flash expiry restores `tint=baseTint` not 0). gameplay: Neon Dragon P2 sets `baseTint=TINT.danger` (enrage red, fire-once, reset on spawn/restart). ui: full-screen skill-activation flash derived from the `useSkills()` ready→fired edge (Freeze blue / Gold Rush gold / Meteor warm), `pointer-events:none` + `aria-hidden`, ~260ms self-dismiss, `prefers-reduced-motion` softened — no new gameplay contract. `bun run check` green (Biome 98 · tsc · Vitest 228/228 · build), ~240KB gz, 5× shuffle clean.
- **2026-06-09** — VFX juice pass 1 landed (engine+gameplay+qa) — **AnimationSystem (slot 8) is now REAL, so the pipeline is 10/10 — no stubs left.** Engine: pooled (§4.4) VFX — particle bursts + floating reward text + hit-flash — driven by AnimationSystem with ZERO per-frame alloc (pre-allocated parallel TypedArrays + swap-remove pool release; pool exhaustion skips, never grows). New `Renderable.flashUntil` field (transient FX tint; base stays 0). Game clock extracted to `engine/clock.ts` to break a loop↔vfx import cycle (`loop` re-exports `gameTime`, backward-compatible). gameplay: hooks via an injectable `VfxSink` (no-op default + spy-tested) — `flashEntity` on a landing hit (NOT a Shade dodge, via `applyDamage` boolean return), death burst + floating gold (`reward×goldMultiplier`), Meteor impact, one-time Neon Dragon P2 enrage burst. **Orchestrator wired `setVfx({...})` in `main.tsx`** after `bootRenderer` (the live connection — sink is no-op without it). qa caught my mis-sorted import (ran tsc but not the full `bun run check`); fixed via `biome --write`, full gate green. `bun run check` green (Biome 94 · tsc · Vitest 204/204 · build), ~239KB gz, 5× shuffle clean. Deferred to pass 2: animated idle glow, freeze/gold-rush screen VFX, dragon persistent enrage tint, per-enemy burst color, §4.5 adaptive-quality.
- **2026-06-09** — 🐲 Neon Dragon final boss + waves 11–20 landed (design-dev+gameplay+qa) — **CAMPAIGN COMPLETE**. Neon Dragon (4000/0.8/300g, sprite 107, isBoss): P2 at 50% HP gains Flying + 1.5× speed (Candy King threshold-phase system extended with `setFlying`, fire-once); **NEW recurring-summon mechanic** — P1 summons a Flutter every 5s via an eid-keyed gameTime timer folded into PathFollow (stops at P2; `resetBossSummon` wired into startGame/restart/_reset for shuffle-safe isolation; zero per-frame alloc). Waves extended 10→20 per §6.3 (Candy King now 5/10/15, Dragon @20); beating wave 20 → existing last-wave `'won'`. NO new §4.2 slot. §6.2 P2 "Plushies on tower death" SKIPPED+flagged (no tower-death mechanic). `bun run check` green (Biome 89 · tsc · Vitest 194/194 · build), ~238KB gz, 5× shuffle clean. **The game is a full playable campaign: start → difficulty → 20 waves of 8 enemy types + 2 bosses → victory/defeat → replay.** Next: Endless mode, towers 3–6, VFX juice, balance tuning.
- **2026-06-09** — Shade + Plushy enemies landed (design-dev+gameplay+qa) — completes the basic-enemy roster (7 of 8 §6.2 types; only Neon Dragon remains). Shade (90/1.6/15g, sprite 105, **20% dodge**) — roll lives in the shared `applyDamage` chokepoint so it covers projectiles + Stormcloud chain + Meteor uniformly; non-dodgers short-circuit (keeps the rng stream deterministic); **injectable RNG** (`setDamageRng`/`resetDamageRng`, default `Math.random`) with reset in `_reset.ts` for shuffle-safe isolation. Plushy (150/0.9/20g, sprite 106, **regen 5 HP/s** in PathFollow, clamped to max, no revive — burst-or-bust tank). Sprinkled into the §6.3 waves 6–8 mix. NO new §4.2 slot. `bun run check` green (Biome 88 · tsc · Vitest 186/186 · build), ~237.5KB gz, 5× shuffle clean (rng reset verified — qa specifically checked the order-dependence hazard).
- **2026-06-08** — Flutter + Splitter enemies landed (design-dev+gameplay+qa), making **waves 1–10 §6.3-faithful**. Flutter (50/2.0/12g, sprite 103, `flying`) — `ENEMY_FLAGS.Flying`; Meteor AoE skips fliers (`if flags&Flying continue`) but single-target projectiles + Stormcloud chain still hit them (chain ≠ ground-splash); hook left for future splash towers. Splitter (160/1.0/22g, sprite 104) **splits on death** — DeathSystem spawns 2 MiniSplitters (40/1.2/8g, reuse 104, no re-split → no recursion; backward-iteration-safe, full-HP minis skip the same-frame death guard) on the flow field. Waves 4/9 now introduce Flutter/Splitter (no longer deferred; counts still tuned). NO new §4.2 slot. `bun run check` green (Biome 87 · tsc · Vitest 178/178 · build), ~237.3KB gz, 5× shuffle clean. Mini-splitter stats flagged tuned (§6.2 silent).
- **2026-06-08** — Candy King mini-boss + waves 4–10 landed (design-dev+gameplay+ui+qa). **First boss + boss-phase system**: CandyKing enemy (1200hp/0.7/100g, sprite id 102); per-entity `Enemy.flags` phase bits, fire-once in DamageSystem — 50% HP → summon 4 Grubs + 1.2× speed, 25% → 1.4× + slow-immune (PathFollow ignores slow for the berserk king). WAVES extended 3→10 (Candy King at 5 & 10; Grub/Snail density ramps; **§6.3 Flutter@4 + Splitter@9 deferred**, per-wave numbers tuned-not-§6.3-exact). Boss HP bar (`snapshot.boss`, progressbar). **Payoff: clearedWaves now reaches 5/10, so the Freeze + Gold Rush skills built last slice are reachable in real play.** NO new §4.2 slot. `bun run check` green (Biome 86 · tsc · Vitest 172/172 · build), ~237KB gz, 5× shuffle clean. **Orchestration note:** a transient API outage froze both tmux agents mid-slice for ~40min; recovered by sending resume nudges (work-in-progress was safe on disk). Deferred/tracked: Flutter/Splitter enemies, waves 11–20 + Neon Dragon, optional boss draw-scale, wave-number playtest tuning.
- **2026-06-08** — Active skills (§6.4) landed (gameplay+ui+qa). 3 skills in `config/skills.ts`: Meteor (tap-to-aim 200 AoE/2-tile, 60s, default), Freeze All (stun-all 3s, 90s, unlock wave 5), Gold Rush (2× gold 10s, 120s, unlock wave 10). ECS skill-state module with `readyAt` gameTime-timestamp cooldowns (no per-frame tick, zero-alloc); `activateSkill` consumed in InputSystem with **aim-beats-placement** priority (Meteor tap targets the battlefield, not tower placement). Folded into existing slots — Freeze **finally implements the unused `Status.stunnedUntil`** (PathFollow freezes stunned enemies), Gold Rush 2× in DeathSystem, snapshot in UISync; NO new §4.2 slot. Skill bar (3 buttons, radial cooldown veil + lock hints) reads `snapshot.skills[]`. ui reconciled to the canonical `SkillType`/`SKILLS` (interim dropped, same pattern as difficulty/PLACEABLE_TOWERS). `bun run check` green (Biome 83 · tsc · Vitest 156/156 · build), ~236.5KB gz, 5× shuffle clean. **Only Meteor reachable in the current 3-wave game** — Freeze/Gold Rush built + unit-tested but locked until wave content reaches 5/10. Flagged: Meteor damage raw-vs-armor (§6.4 says "200 DMG", applied armor-adjusted); `'goldRush'` is the canonical casing.
- **2026-06-08** — Start screen + difficulty select landed (gameplay+ui+qa) — the game's front door. `GamePhase`/`gameStatus` gains `'menu'` (boots frozen via the existing `isSimPaused` guards, board visible); start-screen overlay (title + Casual/Normal/Hardcore radiogroup + Play); `config/difficulty.ts` (§6.5) sets starting gold/lives + enemy HP/speed multipliers (cached mods, zero-alloc — applied in spawnEnemy Health + PathFollow speed) — **finally wires the §6.5 multipliers that had been specced-but-unused since the economy slice**; `startGame(difficulty)`/restart consumed in InputSystem (NO new §4.2 slot); restart keeps the active difficulty. `main.tsx` de-hardcoded — the stale auto-placed Blossom + auto-start removed (players now build their own from the menu). Single `Difficulty` source (config) after a ui reconcile (interim type/table dropped, same pattern as PLACEABLE_TOWERS). `bun run check` green (Biome 75 · tsc · Vitest 130/130 · build), ~235KB gz, 5× shuffle clean.
- **2026-06-08** — 2nd tower **Stormcloud (chain lightning)** landed (design-dev+gameplay+ui+qa, §6.1). `Projectile.special` generalized to bitflags (`Slow=1<<0`/`Chain=1<<1`); TowerAI builds the special from tower config (no longer hardcoded); chain resolves in DamageSystem (primary full + up-to-2 nearest @50% within `CHAIN_RADIUS`, zero-alloc top-2 scan) — NO new §4.2 slot. `tower-stormcloud-l1` sprite (id 2). TowerPicker generalized to a card per `PLACEABLE_TOWERS` (single source of truth in `towers.ts`; ui maps it). `bun run check` green (Biome 69 · tsc · Vitest 112/112 · build), ~234KB gz, 5× shuffle clean. **First strategic tower choice.** Tracked: chain radius (2.5 tiles) NOT-LOCKED — §6.1 gives no arc range, needs a SPEC number; factory-tint guard covers Stormcloud structurally (tint=0 unconditional) but doesn't assert it explicitly.
- **2026-06-07** — M1 wave system landed (gameplay+design-dev+qa, ADR-0003): data-driven waves 1–3 (`waves.ts`), multi-wave SpawnSystem, 3s inter-wave auto-advance + §6.5 wave-clear economy, **Snail** enemy (id 101 sprite by design-dev) + **armor** mechanic, last-wave win guard (no false win after wave 1/2 — caught by test). NO new §4.2 slot. **qa caught a latent visual bug** (flagged via the FX-only-tint seam): factories wrote non-zero design-token colors as base `Renderable.tint`, which RenderSystem multiplied → all atlas sprites mis-colored (Snail blue×purple the giveaway); a bug that had passed every `bun run check` since the visual pass (no test guarded it). Fixed: factories set `tint=0` (native), `cfg.tint` removed, FX-only convention documented + a regression guard test added. `bun run check` green (Biome 68 · tsc · Vitest 94/94 · build), ~233.5KB gz, 5× shuffle clean. **Tracked:** waves 4–20 + bosses; difficulty multipliers; enemy-palette token ADR (design-dev rec).
- **2026-06-07** — **ADR-0003 ACCEPTED: wave system schema + waves 1–3** (fills the SPEC §6.3 gap). Waves are data (`src/game/config/waves.ts`): `Wave = ordered SpawnGroup[]` where each group is `{enemy, count, intervalS, startDelayS}`. Inter-wave auto-advance after 3s; win = last defined wave cleared; wave-clear economy (§6.5) wired. Concrete waves 1–3: W1 8 Grubs@1.0s, W2 12 Grubs@0.8s, W3 10 Grubs@0.7s + 3 Snails trailing. SPEC §6.3 updated with schema + table. Build next (gameplay SpawnSystem generalization + waves.ts + wave-clear economy; ui wave counter).
- **2026-06-07** — M1 game loop COMPLETE (gameplay+ui+qa): win/lose/pause/restart. Authoritative game phase in `src/game/ecs/game-state.ts` (`'playing'|'won'|'lost'`, game-land); win/lose folded into DeathSystem (§6.6) + restart into InputSystem — NO new §4.2 slot. Sim freezes on end via `isSimPaused()` guards (slots 1–7; Render/UISync keep running). `restartGame` full reset + `SpawnSystemHandle.reset()`. ui: "BLOOM PREVAILS" victory + Play Again, restart command store (`src/store/commands.ts`). `bun run check` green (Biome 65 · tsc · Vitest 82/82 · build), ~233KB gz, 5× shuffle clean. Committed as `feat(m1): complete core tower-defense loop`. Remaining M1: real wave system (waves 1–3, §6.3 gap).
- **2026-06-07** — **LOCKED: top-down projection is canonical** (user decision). Renderer, flow-field, and playable proto are top-down; the design bible's isometric tile art is reference-only. Tiles/board render flat (screen px == world px == Position px). SPEC §10 should be confirmed/updated to state top-down; switching to iso later would require an ADR + a renderer projection change.
- **2026-06-07** — M1 visual pass landed (design-dev+engine+gameplay+ui+qa — first 4-agent slice, design-dev via Agent tool + 3 tmux agents). Procedural JSX sprites → runtime SVG atlas (`public/sprites.svg` + per-sprite SVGs ~13.5KB), id-banded registry, `--fs-*` type scale. engine boot-loads textures (`Assets.load` before `startLoop`), RenderSystem draws real Pixi Sprites (tint FX-only, test-safe null-skip in jsdom). gameplay reconciled factory `Renderable.spriteId` to the registry; ui swapped TowerCard to the real glyph + migrated all HUD raw px → `--fs-*`. Green (Biome 60 · Vitest 67/67), ~232.8KB gz. **Flagged:** tiles authored top-down flat (resolved → top-down locked, above).
- **2026-06-07** — M1 interactive tower placement landed (engine+ui+gameplay+qa, first 3-way concurrent fan-out): engine pointer/tap input primitive (lazy self-attach via `ensureAttached`); ui TowerPicker + build-intent store; gameplay real InputSystem (slot 1) tap-to-place with gold spend + flow-field rebuild. Green (Biome 59 · Vitest 65/65), ~219.5KB gz; 5× shuffle clean. Verified gameplay's "main.tsx needs initInput" flag was a FALSE alarm — lazy-attach covers it.
- **2026-06-07** — M1 lives+UISync+HUD slice landed (ui+gameplay+qa): game→React bridge. UISyncSystem (slot 10) pushes ≤10Hz `GameSnapshot` into a new Zustand store (snapshot-only mirror — game state authoritative in ECS); first real HUD + "BASTION FELL" defeat. PathFollow goal-leak calls `loseLives()`. Green (Biome 51 · Vitest 45/45), ~218KB gz; 5× shuffle clean. First **ui** agent slice. Surfaced the font-size design-token gap (later closed).
- **2026-06-07** — M1 combat slice landed (engine+gameplay+qa): TowerAI→Projectile→Damage→Death (§4.2 slots 4-7); `gameTime()` clock; pool-integrated factories (Hit tag); `Resources{gold,lives}` §6.5 Normal (150/20). Green (Biome 44 · Vitest 33/33), ~217KB gz. **qa caught a real order-dependent bug** — towers leaked into the shared world singleton across tests (no `releaseTower`/reset drain); fixed + verified by 5× shuffle.
- **2026-06-07** — M1 vertical slice landed (engine+gameplay+qa): ECS world + RAF loop + locked §4.2 pipeline, flow-field (Dijkstra, not A*), RenderSystem, object pools, Spawn + PathFollow, Grub/Blossom config — grub walks the lane, Blossom rendered. Green (Biome 33 · Vitest 25/25), ~204KB gz. Surfaced the SPEC §6.3 per-wave gap.
- **2026-06-05** — M0 scaffold landed: Bun+Vite6+React19+Pixi v8 boots grid canvas; green (~198KB gz JS).
- **2026-06-05** — ADR-0002: adopted designer's exported tokens + type (Baloo 2/Nunito) as visual source of truth; SPEC §2.3/§2.4 updated. Audio/biomes/i18n/difficulty-naming flagged as open decisions.
- **2026-06-05** — Added tmux multi-agent orchestration (`scripts/`, `~/.claude/profiles/bloom-*.md`).
- **2026-06-05** — Added 6 project subagents in `.claude/agents/` (engine-dev, gameplay-dev, ui-dev, perf-guardian, test-engineer, spec-reviewer).
- **2026-06-05** — Stack modernized: Bun (runtime/PM) + Vite 6 + React 19 + Biome; Bun runs Vite, not replaces it.
- **2026-06-05** — Design phase complete, assets in `design-assets/`.
- **2026-06-05** — Locked tech stack (Vite/Pixi/bitECS/Rive/React-UI-only).
- **2026-06-05** — SPEC.md committed as source of truth.
