---
description: Clean build artifacts and reset
---

Run these cleanup steps:

1. Remove `node_modules/`, `dist/`, `.vite/`, `coverage/`
2. Remove any `.DS_Store` files
3. Show before/after disk usage
4. Ask if I want to `bun install` fresh

DO NOT touch `src/`, `design-assets/`, or any committed files.
